# Fase 0, cierre: defectos B6, B7 y B8 — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** cerrar la Fase 0 con los tres defectos que salieron de las mediciones M1-M6
([ADR 0016](../../adr/0016-what-desktop-reads-from-a-profile.md), «Consecuencias»):

- **B6**: lo que se cambia con `/config` dentro de un perfil se pierde en el siguiente `ccp profile sync`.
- **B7**: `ccp profile rename` deja un perfil official sin login y no lo dice.
- **B8**: `ccp profile add` no genera la config del perfil, aunque dice que sí.

**Architecture:** los tres son arreglos en `internal/core` con su cara en `cli`, `tui`, `serve` y `gui`.
- **B8 va primero** porque es la base de B6: a partir de él, un perfil nace con su `settings.json`
  generado y con la copia de lo generado, que es la línea base con la que se detecta la deriva.
- **B7** es independiente: un campo más en lo que devuelve `ProfileRename`.
- **B6** va en cuatro pasos:
  1. un motor puro de diff;
  2. la copia de lo último generado, en `profiles/<n>/state/`;
  3. la adopción dentro de `CfgRegenerate`;
  4. la superficie (`profile sync`, `serve`).
- La GUI y la documentación cierran.

**Tech Stack:** Go 1.24 · bubbletea (TUI) · React/TS (GUI) · bash (oráculo `legacy/`, sin cambios).

**Spec:** `docs/superpowers/specs/2026-09-18-config-unificada-snapshots-nube-design.md` §1 y §4, y el
ADR 0016 (M4 para B7; la deriva de `/config` para B6).

## Global Constraints

- **Dónde:** worktree `/Users/joseizaguirre/Documents/Personal/proyectos/ccp/.claude/worktrees/snapshots-locales`,
  rama `fix/fase0-defectos`, ya activa.
  - No se cambia de rama ni se hace `stash` ni `push`.
  - No se toca el checkout principal ni otra rama o worktree.
- **Commits: autorizados por el usuario para esta fase.** Cada tarea termina en su commit, con los tests
  en verde y un mensaje en español estilo conventional commits. Sin trailers `Co-Authored-By` ni
  atribución a herramientas.
- **TDD:** primero el test, comprobar que falla, después el código.
- **Gates de CI, todos verdes al cerrar cada tarea que toque Go:**
  - `gofmt -l internal cmd` vacío;
  - `go vet ./...`;
  - `go test ./...`;
  - `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run --timeout 5m`;
  - `bash legacy/tests/run.sh`;
  - `bash testdata/golden/capture.sh --check`.

  Si se toca `gui/`, también `(cd gui && npm run typecheck)`. `gui/src-tauri` no se toca, y aquí `cargo`
  no está instalado.
- **Bilingüe:**
  - los textos nuevos de la CLI van en `internal/core/i18n/catalog_cli.go` con `En` y `Es`;
  - los de la TUI, en `catalog_tui.go`;
  - los de la GUI, en español dentro de `t('…')` y su inglés en `gui/src/lib/i18n_en.ts`.
  - `TestCatalogComplete` exige las dos lenguas.
- **Los tests nunca tocan el estado real:** `CCP_HOME`, `CCP_CLAUDE_SRC`, `HOME` y
  `CCP_DESKTOP_DEFAULT_DATA_DIR` en temporales. Nada contra `~/.config/ccp`, `~/.claude`, `~/.claude.json`,
  el data dir de Claude Desktop ni el Llavero.
- **Contrato golden intacto.** Ninguna tarea toca `_env`, `_hook`, `resolve`, `path test`,
  `status --json`, la completion ni `completion-shellinit`.
  - **El oráculo bash no cambia:** su `_seed_cc_home` ya genera la config, y B8 alinea Go con él.
  - `profile sync` y `profile rename` quedan fuera del contrato congelado.
- **`go.mod` sigue en `go 1.24.0`.** Estas tareas no añaden dependencias.
- **`serve`: el protocolo sigue en `1`.** Hoy `profiles.sync` y `profiles.rename` devuelven `{"ok":true}`
  (`serve.go` pone `okResult` cuando un método devuelve `nil`). Pasan a devolver `{"ok":true, …}` con un
  campo más: se añade, no cambia la forma.
- Sin red, salvo módulos Go o paquetes npm. Nada de Dokploy ni despliegues. Ningún secreto se imprime ni
  se commitea.

## Decisiones que el plan fija antes de tocar código

**B6 — la adopción:**

1. **Vive dentro de `CfgRegenerate`, para todos los caminos.** Son `profile sync`, `profile config`,
   `instruct add`, `overlay.envSet` de la GUI, los restores y el rename. Si solo adoptara `profile sync`,
   cualquier otro camino que regenerase perdería el `/config` igual que hoy.
   - Contar lo adoptado es opcional: `CfgRegenerateReport` y `ProfileSyncReport` lo devuelven.
   - `CfgRegenerate` y `ProfileSync` siguen con su firma, como envoltorios que descartan el informe. Tienen
     unos quince llamadores que no lo necesitan.
   - Los restores (B2 y snapshots) también adoptan. La deriva es la única copia de esa decisión del
     usuario: ni el snapshot ni el backup capturan `cc-home/settings.json`, que es derivado.
2. **La línea base es la copia de lo último generado:** `profiles/<n>/state/last-settings.json`, en
   `0600`, escrita con tmp+rename.
   - **Fuera del cc-home**, porque Claude Code lo lee entero.
   - **Fuera de `overlay/`**, porque es authored y lo capturan snapshots y backups.
   - `SnapshotSources` y `BackupExport` enumeran rutas explícitas, así que no la capturan. Un test lo fija.
   - Cuelga de `profiles/<n>/`: se mueve con el rename y se borra con el `rm`.
   - **Si no se puede escribir, se borra la vieja y se devuelve el error.** Una copia desfasada es peor
     que ninguna: la siguiente regeneración tomaría por deriva los cambios del global y los congelaría en
     el overlay.
3. **Deriva = `diff(copia, cc-home actual)`, comparando valores decodificados.**
   - El diff es profundo: recursa cuando los dos lados son objetos, y los arrays y escalares se comparan
     enteros, con la misma semántica que `MergeJSON`.
   - Los números se comparan por valor (`big.Rat`): Claude Code reescribe el archivo entero con
     `JSON.stringify`, que convierte `30.0` en `30`, y eso no es un cambio del usuario.
4. **La capa auto nunca es deriva.** Antes del diff se quitan de los dos lados:
   - el `statusLine` que es nuestro envoltorio (`isCCPStatusLine`);
   - las entradas de `hooks.StopFailure` que llaman a `_limit-hook` (`stripAutoLayer`, junto a sus
     hermanas en `autohooks.go`).

   Así no se adopta ni aunque las dos copias difieran en la ruta del binario. Un `statusLine` que el
   usuario cambió con `/statusline` sí se adopta, y la capa lo envuelve en la regeneración.
5. **Solo se adopta lo que la regeneración perdería.** Un cambio cuyo valor ya sale de global ⊕ overlay ⊕
   auto no se copia al overlay.
   - Esto evita congelar el global en el overlay cuando la copia está desfasada, por ejemplo porque un
     binario anterior a B6 regeneró entre medias.
6. **Conflicto: gana el overlay.** Si la clave cambió a mano **y** también en el overlay desde la última
   regeneración (el valor del overlay ya no es el de la copia), no se adopta y se avisa. Es el caso de un
   `ccp profile config` recién editado.
7. **Lo que el usuario quitó solo se avisa**, y solo si va a volver. El overlay no puede expresar un
   borrado sin un `null`, y Claude Code no lo espera.
8. **`cc-home/settings.json` inválido:** no se adopta nada.
   - Se guarda una copia en `state/settings.invalid.json` y se avisa con su ruta. Después se regenera.
   - Si esa copia no se puede guardar, se aborta sin pisar el archivo.
9. **Sin copia previa no se adopta nada:** se guarda la copia y ya. Es el caso de un perfil de antes de
   este arreglo, o de una copia ilegible. El primer `ccp upgrade` crea la línea base, porque termina en un
   sync.
10. **El overlay se reescribe con tmp+rename y conserva su modo.** Si es un symlink (un repo de
    dotfiles), se escribe en su destino y el enlace sigue siendo un enlace. Sale ordenado por clave, como
    ya pasa con `OverlayEnvSet`.

**B7 — el aviso del rename:**

- **`ProfileRename` cambia de firma** a `(RenameResult, error)`. Solo tiene tres llamadores (CLI, TUI y
  `serve`), y los tres tienen que enseñar el aviso: un envoltorio dejaría olvidarlo.
- **`Relogin` se decide antes de mover.** Vale `official && HasLogin(home, viejo)`.
- **ccp no toca el Llavero:** ni mueve la credencial ni la lee.
- **No se intenta distinguir Linux**, donde la credencial está en `.credentials.json` dentro del cc-home y
  sí viajaría: no está medido. Un aviso de más cuesta un `/login`; uno de menos, un perfil que no entra.
- **Limitación conocida, fuera de alcance:** tras el rename, `HasLogin` sigue diciendo «configurado». El
  `oauthAccount` viaja con el `.claude.json`, pero la credencial del Llavero ya no se encuentra. El aviso
  del rename es la señal.

---

### Task 1: B8 — `ccp profile add` genera la config

`ProfileAddOfficial` y `ProfileAddProvider` (`internal/core/profile.go`) solo llaman a `seedCCHome`. El
oráculo (`_seed_cc_home`, `legacy/bin/ccp:167`) además llama a `ccp_cfg_init_overlay` y a
`ccp_cfg_regenerate`. `CfgRegenerate` ya empieza por `CfgInitOverlay`, así que basta con llamarla.
`ccp.yaml` ya está guardado cuando corre: `applyAutoLayer` lo lee.

Comprobado antes de escribir el plan: con este cambio, `go test ./...` sigue en verde. Solo hay dos tests
que ahora leerían el `~/.claude` real, y hay que aislarlos.

**Files:**
- Modify: `internal/core/profile.go` (`ProfileAddOfficial`, `ProfileAddProvider`, el comentario de `seedCCHome`)
- Modify: `internal/core/profile_test.go` (test nuevo)
- Modify: `internal/core/effective_test.go` (comentario de `seedEff`, que ya no es verdad)
- Modify: `internal/cli/config_edit_test.go` (`CCP_CLAUDE_SRC` en `TestConfigEditProfileNoBloqueanteNoMiente` y `TestConfigEditProfileBloqueanteSiRegenera`)

- [ ] **Step 1: El test (`profile_test.go`)**

```go
// Un perfil nace con su config generada, como en el oráculo (_seed_cc_home):
// overlay/, cc-home/CLAUDE.md y cc-home/settings.json. Antes no los tenía hasta
// el primer `ccp profile sync`, aunque el CLI dijera «config generada» (B8).
func TestProfileAddGeneraLaConfig(t *testing.T) {
	for _, kind := range []string{"official", "deepseek"} {
		t.Run(kind, func(t *testing.T) {
			home, src := t.TempDir(), t.TempDir()
			t.Setenv("CCP_CLAUDE_SRC", src)
			if err := os.WriteFile(filepath.Join(src, "settings.json"), []byte(`{"model":"opus"}`), 0o644); err != nil {
				t.Fatal(err)
			}
			if err := os.WriteFile(filepath.Join(src, "CLAUDE.md"), []byte("global\n"), 0o644); err != nil {
				t.Fatal(err)
			}
			var err error
			if kind == "official" {
				err = ProfileAddOfficial(home, "p")
			} else {
				err = ProfileAddDeepseek(home, "p", BuiltinDefaults())
			}
			if err != nil {
				t.Fatal(err)
			}
			for _, f := range []string{cfgInstrFile(home, "p"), cfgSettingsFile(home, "p")} {
				if !fileExists(f) {
					t.Errorf("falta %s", f)
				}
			}
			cch := ccHomePath(home, "p")
			md, err := os.ReadFile(filepath.Join(cch, "CLAUDE.md"))
			if err != nil || !strings.Contains(string(md), "@"+cfgInstrFile(home, "p")) ||
				!strings.Contains(string(md), "@"+filepath.Join(src, "CLAUDE.md")) {
				t.Errorf("cc-home/CLAUDE.md sin los @import: %q %v", md, err)
			}
			sj, err := os.ReadFile(filepath.Join(cch, "settings.json"))
			if err != nil || !strings.Contains(string(sj), "opus") {
				t.Errorf("cc-home/settings.json no sale del global: %q %v", sj, err)
			}
		})
	}
}
```

Run: `go test ./internal/core/ -run TestProfileAddGeneraLaConfig`
Expected: FAIL (falta `overlay/CLAUDE.md`).

- [ ] **Step 2: El arreglo — `internal/core/profile.go`**

En las dos funciones, sustituye `return seedCCHome(home, name)` por:

```go
	if err := seedCCHome(home, name); err != nil {
		return err
	}
	return profileGenerateConfig(home, name)
```

Y añade, junto a `seedCCHome`:

```go
// profileGenerateConfig es la segunda mitad de _seed_cc_home del oráculo:
// overlay vacío y cc-home regenerado (CfgRegenerate empieza por CfgInitOverlay).
// Sin esto el perfil nacía sin overlay/, sin cc-home/CLAUDE.md y sin
// settings.json hasta el primer sync, aunque el CLI dijera «config generada»
// (spec 2026-09-18, B8). ccp.yaml ya está guardado, que es lo que lee la capa
// auto. Si falla, el perfil existe igual (como en el bash): se dice cómo
// reintentarlo en vez de deshacer el alta.
func profileGenerateConfig(home, name string) error {
	src, err := claudeSrc()
	if err != nil {
		return err
	}
	if err := CfgRegenerate(home, name, src); err != nil {
		return fmt.Errorf("perfil %q creado, pero no se pudo generar su config (reintenta con: ccp profile sync %s): %w", name, name, err)
	}
	return nil
}
```

En el comentario de `seedCCHome`, cambia «CLAUDE.md y settings.json son responsabilidad de cfg (fuera de
este issue)» por «CLAUDE.md y settings.json los genera cfg (CfgRegenerate), que el alta llama justo
después (profileGenerateConfig)».

- [ ] **Step 3: Los tests que asumían lo contrario**

- `effective_test.go`, en `seedEff`: el comentario que dice «ProfileAddOfficial solo crea
  profiles/<name>/cc-home/ … el directorio overlay/ lo crea CfgInitOverlay» ya no es verdad. Déjalo así:
  `// ProfileAddOfficial ya crea overlay/ (B8); CfgInitOverlay es idempotente y se queda por claridad.`
  La llamada se mantiene.
- `config_edit_test.go`: añade `t.Setenv("CCP_CLAUDE_SRC", t.TempDir())` justo después del
  `t.Setenv("CCP_HOME", home)` de `TestConfigEditProfileNoBloqueanteNoMiente` y de
  `TestConfigEditProfileBloqueanteSiRegenera`. Ahora el alta regenera, y sin esto leerían el
  `~/.claude/settings.json` real.

- [ ] **Step 4: Comprobar**

Run: `go test ./internal/... && gofmt -l internal cmd && go vet ./...`
Expected: PASS, y `gofmt` no imprime nada.

- [ ] **Step 5: Commit**

`fix(profile): profile add genera overlay, CLAUDE.md y settings.json como el oráculo (B8)`

---

### Task 2: B7 — el rename avisa de que hay que volver a iniciar sesión

El Llavero guarda la credencial como `Claude Code-credentials-<sha256(CLAUDE_CONFIG_DIR)[:8]>` (ADR 0016,
M4), y el rename mueve el cc-home. ccp no toca el Llavero: avisa.

**Files:**
- Modify: `internal/core/profile_rename.go` (`RenameResult`, nueva firma de `ProfileRename`)
- Modify: `internal/core/profile_rename_test.go` (los ocho `ProfileRename(` pasan a `_, err`, más tests nuevos)
- Modify: `internal/cli/profile.go` (`profileRename`)
- Modify: `internal/cli/profile_rename_test.go`
- Modify: `internal/tui/forms.go` (`applyRename`), `internal/tui/handoff_test.go`
- Modify: `internal/cli/serve_methods.go` (`srvProfilesRename`), `internal/cli/serve_test.go`
- Modify: `internal/core/i18n/catalog_cli.go`, `internal/core/i18n/catalog_tui.go`

- [ ] **Step 1: Los tests de core (`profile_rename_test.go`)**

```go
// Un perfil official con sesión pierde el login al renombrarlo: Claude Code
// guarda la credencial con un nombre que sale de la ruta del cc-home (ADR 0016,
// M4). ccp no toca el Llavero, así que lo dice (B7).
func TestProfileRenameAvisaSiHabiaLogin(t *testing.T) {
	cases := []struct {
		nombre, tipo, claudeJSON string
		quiero                   bool
	}{
		{"official con cuenta", "official", `{"oauthAccount":{"emailAddress":"a@b"}}`, true},
		{"official sin login", "official", `{}`, false},
		{"proveedor", "deepseek", `{"oauthAccount":{"emailAddress":"a@b"}}`, false},
	}
	for _, tc := range cases {
		t.Run(tc.nombre, func(t *testing.T) {
			home := seedRenameHome(t)
			t.Setenv("CCP_CLAUDE_SRC", t.TempDir())
			if tc.tipo != "official" {
				c, err := Load(home)
				if err != nil {
					t.Fatal(err)
				}
				c.Profiles["viejo"] = Profile{Type: tc.tipo, BaseURL: "https://x", ModelPro: "m", ModelFlash: "m", Effort: "high"}
				if err := Save(home, c); err != nil {
					t.Fatal(err)
				}
			}
			cj := filepath.Join(ccHomePath(home, "viejo"), ".claude.json")
			if err := os.WriteFile(cj, []byte(tc.claudeJSON), 0o600); err != nil {
				t.Fatal(err)
			}
			res, err := ProfileRename(home, "viejo", "nuevo")
			if err != nil {
				t.Fatal(err)
			}
			if res.Relogin != tc.quiero {
				t.Errorf("Relogin = %v, quiero %v", res.Relogin, tc.quiero)
			}
		})
	}
}
```

Adapta las ocho llamadas existentes, que es mecánico (`if err :=` → `if _, err :=`, `err :=` →
`_, err :=`, `err =` → `_, err =`):

```bash
sed -i '' -e 's/if err := ProfileRename(/if _, err := ProfileRename(/' \
          -e 's/^\([[:space:]]*\)err := ProfileRename(/\1_, err := ProfileRename(/' \
          -e 's/^\([[:space:]]*\)err = ProfileRename(/\1_, err = ProfileRename(/' internal/core/profile_rename_test.go
grep -n 'ProfileRename(' internal/core/profile_rename_test.go   # las 8 + las nuevas, todas con dos valores
# (probado sobre una copia: líneas 55, 115, 146, 177, 234, 337, 387 y 445)
```

Run: `go test ./internal/core/ -run ProfileRename`
Expected: FAIL de compilación (`ProfileRename` devuelve un solo valor).

- [ ] **Step 2: El arreglo — `internal/core/profile_rename.go`**

```go
// RenameResult es lo que el rename no puede arreglar solo y el usuario tiene que
// saber. Vale también junto a un error: si el directorio ya se movió y lo que
// falló fue la regeneración, el login se perdió igual.
type RenameResult struct {
	// Relogin: el perfil era official y tenía sesión. Claude Code guarda su
	// credencial en el Llavero con un nombre que sale de la ruta del cc-home
	// (`Claude Code-credentials-<sha256(dir)[:8]>`, ADR 0016, M4), y el rename
	// acaba de cambiar esa ruta: hay que volver a hacer /login. ccp no mueve la
	// credencial, porque tocaría secretos del Llavero.
	Relogin bool
}
```

La firma pasa a `func ProfileRename(home, oldName, newName string) (RenameResult, error)`. Cada
`return fmt.Errorf(…)` y cada `return err` anteriores al movimiento devuelven `RenameResult{}`. Justo
antes de `movido := false`:

```go
	// Se mira ANTES de mover: después, el .claude.json ya está en la ruta nueva.
	res := RenameResult{Relogin: c.Profiles[oldName].Type == "official" && HasLogin(home, oldName)}
```

- Las salidas de error posteriores al movimiento (Save, handoffs) devuelven `RenameResult{}`, porque
  el directorio se devuelve a su sitio.
- La de `ProfileSync` devuelve `res` junto al error, porque el rename ya es válido.
- El final es `return res, nil`.
- Añade al comentario de la función un párrafo: «Tampoco puede mover el login de un perfil official…
  (ver RenameResult)».

- [ ] **Step 3: CLI — `internal/cli/profile.go` y su catálogo**

`catalog_cli.go`, junto a `cli.profile.rename_active_hint`:

```go
	"cli.profile.rename_relogin_hint": {
		En: "  Claude Code keeps this profile's login under a name derived from its folder, and the rename changed that folder: log in again with 'ccp profile login %s'. The old credential stays in the Keychain; ccp doesn't touch it.",
		Es: "  Claude Code guarda el login de este perfil con un nombre que sale de su carpeta, y el rename la cambió: vuelve a iniciar sesión con 'ccp profile login %s'. La credencial vieja se queda en el Llavero; ccp no la toca.",
	},
```

En `profileRename`:

```go
	res, err := core.ProfileRename(home, old, nuevo)
	if err != nil {
		fmt.Fprintf(stderr, "[error] %v\n", err)
		if res.Relogin {
			fmt.Fprintln(stderr, i18n.T(lang, "cli.profile.rename_relogin_hint", nuevo))
		}
		return 1
	}
	fmt.Fprintln(stdout, okLine(stdout, i18n.T(lang, "cli.profile.renamed", old, nuevo)))
	if res.Relogin {
		fmt.Fprintln(stdout, i18n.T(lang, "cli.profile.rename_relogin_hint", nuevo))
	}
```

El aviso de `CCP_PROFILE` y el del lanzador quedan detrás, sin cambios.

Test (`internal/cli/profile_rename_test.go`):

```go
func TestProfileRenameCLIAvisaDelLogin(t *testing.T) {
	home := seedCLIProfile(t)
	t.Setenv("CCP_HOME", home)
	t.Setenv("CCP_LANG", "es")
	t.Setenv("CCP_CLAUDE_SRC", t.TempDir())
	t.Setenv("CCP_PROFILE", "")
	cj := filepath.Join(home, "profiles", "viejo", "cc-home", ".claude.json")
	if err := os.WriteFile(cj, []byte(`{"oauthAccount":{"emailAddress":"a@b"}}`), 0o600); err != nil {
		t.Fatal(err)
	}
	var out, errb bytes.Buffer
	if code := Dispatch([]string{"profile", "rename", "viejo", "nuevo"}, &out, &errb); code != 0 {
		t.Fatalf("exit %d: %s", code, errb.String())
	}
	if !strings.Contains(out.String(), "ccp profile login nuevo") {
		t.Errorf("no avisa de que hay que volver a iniciar sesión: %q", out.String())
	}
}
```

Y en `TestProfileRenameCLI`, que no tiene login, añade la aserción contraria: la salida no contiene
`profile login`.

- [ ] **Step 4: TUI — `applyRename` y su catálogo**

`catalog_tui.go`, junto a `tui.form.profile_renamed`:

```go
	"tui.form.profile_renamed_relogin": {
		En: "Profile renamed: %s → %s. Its login stayed with the old folder: ccp profile login %s",
		Es: "Perfil renombrado: %s → %s. Su login se quedó con la carpeta vieja: ccp profile login %s",
	},
```

```go
func applyRename(home, old, nuevo string, lang i18n.Lang) (string, error) {
	res, err := core.ProfileRename(home, old, nuevo)
	if err != nil {
		return "", err
	}
	if res.Relogin {
		return i18n.T(lang, "tui.form.profile_renamed_relogin", old, nuevo, nuevo), nil
	}
	return i18n.T(lang, "tui.form.profile_renamed", old, nuevo), nil
}
```

Test en `handoff_test.go`, junto a `TestApplyRenameDesdeLaTUI`: el mismo `Save` de `viejo` official, más
`profiles/viejo/cc-home/.claude.json` con `{"oauthAccount":{"emailAddress":"a@b"}}`. Se espera que
`msg` contenga `ccp profile login nuevo`.

- [ ] **Step 5: serve — `srvProfilesRename`**

```go
	res, err := core.ProfileRename(s.home, p.From, strings.TrimSpace(p.To))
	if err != nil {
		return nil, err
	}
	// relogin: la GUI lo enseña al confirmar (B7). "ok" se queda para no cambiar la
	// forma de lo que ya se respondía.
	return map[string]any{"ok": true, "relogin": res.Relogin}, nil
```

Test (`serve_test.go`):

```go
func TestServeProfilesRenameDiceSiHayQueVolverAEntrar(t *testing.T) {
	home := serveEnv(t)
	cj := filepath.Join(home, "profiles", "work", "cc-home", ".claude.json")
	if err := os.WriteFile(cj, []byte(`{"oauthAccount":{"emailAddress":"a@b"}}`), 0o600); err != nil {
		t.Fatal(err)
	}
	_, r := serveRun(t, req(1, "profiles.rename", map[string]any{"from": "work", "to": "work2"}))
	var got struct {
		OK      bool `json:"ok"`
		Relogin bool `json:"relogin"`
	}
	mustResult(t, r["1"], &got)
	if !got.OK || !got.Relogin {
		t.Fatalf("profiles.rename = %+v, quiero ok y relogin", got)
	}
}
```

- [ ] **Step 6: Comprobar**

Run: `go test ./internal/... && gofmt -l internal cmd && go vet ./...`
Expected: PASS.

- [ ] **Step 7: Commit**

`fix(profile): el rename avisa de que hay que volver a iniciar sesión (B7)`

---

### Task 3: B6 (1/4) — el motor puro de la deriva

Todo lo que compara y toca rutas dentro de un JSON decodificado, sin disco: se prueba con tablas.

**Files:**
- Create: `internal/core/cfg_drift.go`
- Create: `internal/core/cfg_drift_test.go`
- Modify: `internal/core/autohooks.go` (`stripAutoLayer`), `internal/core/autohooks_test.go`

- [ ] **Step 1: Los tests**

`cfg_drift_test.go`:

```go
func obj(t *testing.T, s string) map[string]any {
	t.Helper()
	m, err := decodeJSONObject([]byte(s))
	if err != nil {
		t.Fatalf("%s: %v", s, err)
	}
	return m
}

func TestDiffSettings(t *testing.T) {
	type ch struct {
		path string
		kind changeKind
	}
	cases := []struct {
		nombre, last, cur string
		quiero            []ch
	}{
		{"nada", `{"a":1}`, `{"a":1}`, nil},
		{"añadida", `{}`, `{"autoCompactEnabled":false}`, []ch{{"autoCompactEnabled", changeAdded}}},
		{"anidada", `{"env":{"A":"1"}}`, `{"env":{"A":"1","B":"2"}}`, []ch{{"env.B", changeAdded}}},
		{"cambiada", `{"model":"opus"}`, `{"model":"sonnet"}`, []ch{{"model", changeChanged}}},
		{"array entero", `{"permissions":{"allow":["a"]}}`, `{"permissions":{"allow":["a","b"]}}`, []ch{{"permissions.allow", changeChanged}}},
		{"quitada", `{"env":{"A":"1"}}`, `{"env":{}}`, []ch{{"env.A", changeRemoved}}},
		{"número por valor", `{"n":30.0,"m":1e2}`, `{"n":30,"m":100}`, nil},
		{"cambio de tipo", `{"x":{"a":1}}`, `{"x":"s"}`, []ch{{"x", changeChanged}}},
		{"orden estable", `{}`, `{"b":1,"a":1}`, []ch{{"a", changeAdded}, {"b", changeAdded}}},
	}
	for _, tc := range cases {
		t.Run(tc.nombre, func(t *testing.T) {
			var got []ch
			for _, c := range diffSettings(obj(t, tc.last), obj(t, tc.cur)) {
				got = append(got, ch{pathString(c.Path), c.Kind})
			}
			if !reflect.DeepEqual(got, tc.quiero) {
				t.Errorf("diff = %v, quiero %v", got, tc.quiero)
			}
		})
	}
}

func TestJSONSetPathYLookup(t *testing.T) {
	doc := obj(t, `{"a":"x"}`)
	jsonSetPath(doc, []string{"a", "b", "c"}, true)
	if v, ok := jsonLookup(doc, []string{"a", "b", "c"}); !ok || v != true {
		t.Fatalf("lookup tras set = %v %v (doc %v)", v, ok, doc)
	}
	if _, ok := jsonLookup(doc, []string{"a", "z"}); ok {
		t.Error("una ruta que no existe no puede encontrarse")
	}
}

func TestDecodeJSONObjectRechazaLoQueNoEsObjeto(t *testing.T) {
	for _, s := range []string{"", "{roto", "[]", "null", `{} basura`} {
		if _, err := decodeJSONObject([]byte(s)); err == nil {
			t.Errorf("%q tenía que fallar", s)
		}
	}
}
```

`autohooks_test.go`:

```go
// La capa de sensores es infraestructura de ccp: la deriva de /config no puede
// adoptarla nunca al overlay (B6). stripAutoLayer quita solo lo nuestro.
func TestStripAutoLayerQuitaSoloLoNuestro(t *testing.T) {
	SetAutoHooksBin("ccp")
	doc := map[string]any{
		"model":      "opus",
		"statusLine": map[string]any{"type": "command", "command": "/otro/sitio/ccp _statusline -- /bin/sh -c starship"},
		"hooks": map[string]any{
			"StopFailure": []any{
				map[string]any{"hooks": []any{map[string]any{"type": "command", "command": "/otro/sitio/ccp _limit-hook"}}},
				map[string]any{"hooks": []any{map[string]any{"type": "command", "command": "notify-send fallo"}}},
			},
			"PreToolUse": []any{"x"},
		},
	}
	got := stripAutoLayer(doc)
	if _, ok := got["statusLine"]; ok {
		t.Error("el statusLine de ccp (aunque el binario se haya mudado) se tiene que quitar")
	}
	hooks := got["hooks"].(map[string]any)
	if sf := hooks["StopFailure"].([]any); len(sf) != 1 || !strings.Contains(fmt.Sprint(sf[0]), "notify-send") {
		t.Errorf("StopFailure ajeno perdido o el nuestro conservado: %v", sf)
	}
	if _, ok := hooks["PreToolUse"]; !ok {
		t.Error("los demás eventos no se tocan")
	}
	if _, ok := doc["statusLine"]; !ok || len(doc["hooks"].(map[string]any)["StopFailure"].([]any)) != 2 {
		t.Error("stripAutoLayer no puede mutar su entrada")
	}
	ajeno := map[string]any{"statusLine": map[string]any{"type": "command", "command": "~/.claude/hooks/cc_statusline.sh"}}
	if _, ok := stripAutoLayer(ajeno)["statusLine"]; !ok {
		t.Error("un statusLine ajeno no es nuestro")
	}
	solo := map[string]any{"hooks": map[string]any{"StopFailure": []any{
		map[string]any{"hooks": []any{map[string]any{"type": "command", "command": "ccp _limit-hook"}}},
	}}}
	if _, ok := stripAutoLayer(solo)["hooks"]; ok {
		t.Error("sin nada más, hooks tiene que desaparecer entero")
	}
}
```

Run: `go test ./internal/core/ -run 'DiffSettings|JSONSetPath|DecodeJSONObject|StripAutoLayer'`
Expected: FAIL de compilación.

- [ ] **Step 2: `internal/core/cfg_drift.go` (la parte pura)**

```go
package core

// cfg_drift.go — la deriva de cc-home/settings.json (spec 2026-09-18, B6).
//
// cc-home/settings.json lo genera ccp (global ⊕ overlay ⊕ auto), pero es también
// el settings.json «de usuario» del Claude Code de ese perfil: /config, /model o
// /permissions escriben ahí (ADR 0016). Sin este archivo, la siguiente
// regeneración lo pisaba y el cambio se perdía sin avisar. Aquí vive el diff
// puro; la adopción, que sí toca disco, está en adoptSettingsDrift.

import (
	"encoding/json"
	"fmt"
	"math/big"
	"slices"
	"sort"
	"strings"
)

type changeKind int

const (
	changeAdded changeKind = iota
	changeChanged
	changeRemoved
)

// settingsChange es una diferencia entre lo que ccp generó y lo que hay ahora.
// Path es la ruta de claves; Value, el valor actual (nil si se quitó).
type settingsChange struct {
	Path  []string
	Kind  changeKind
	Value any
}

// diffSettings compara lo último generado (last) con lo que hay ahora (cur), con
// la semántica de MergeJSON: recursa solo cuando los dos lados son objetos; un
// array o un escalar distinto es un cambio de la clave entera. Ordenado por ruta
// para que el informe y los tests sean estables.
func diffSettings(last, cur map[string]any) []settingsChange {
	var out []settingsChange
	diffInto(&out, nil, last, cur)
	sort.Slice(out, func(i, j int) bool { return pathString(out[i].Path) < pathString(out[j].Path) })
	return out
}

func diffInto(out *[]settingsChange, prefix []string, last, cur map[string]any) {
	for k, cv := range cur {
		p := append(slices.Clone(prefix), k)
		lv, ok := last[k]
		if !ok {
			*out = append(*out, settingsChange{Path: p, Kind: changeAdded, Value: cv})
			continue
		}
		lm, lok := lv.(map[string]any)
		cm, cok := cv.(map[string]any)
		if lok && cok {
			diffInto(out, p, lm, cm)
			continue
		}
		if !jsonEqual(lv, cv) {
			*out = append(*out, settingsChange{Path: p, Kind: changeChanged, Value: cv})
		}
	}
	for k := range last {
		if _, ok := cur[k]; !ok {
			*out = append(*out, settingsChange{Path: append(slices.Clone(prefix), k), Kind: changeRemoved})
		}
	}
}

// jsonEqual compara dos valores decodificados con UseNumber. Los números van por
// valor y no por su literal: Claude Code reescribe el archivo entero con
// JSON.stringify, que convierte 30.0 en 30, y eso no es un cambio del usuario.
func jsonEqual(a, b any) bool {
	switch x := a.(type) {
	case map[string]any:
		y, ok := b.(map[string]any)
		if !ok || len(x) != len(y) {
			return false
		}
		for k, v := range x {
			if w, ok := y[k]; !ok || !jsonEqual(v, w) {
				return false
			}
		}
		return true
	case []any:
		y, ok := b.([]any)
		if !ok || len(x) != len(y) {
			return false
		}
		for i := range x {
			if !jsonEqual(x[i], y[i]) {
				return false
			}
		}
		return true
	case json.Number:
		y, ok := b.(json.Number)
		if !ok {
			return false
		}
		if x == y {
			return true
		}
		rx, okx := new(big.Rat).SetString(string(x))
		ry, oky := new(big.Rat).SetString(string(y))
		return okx && oky && rx.Cmp(ry) == 0
	default:
		return a == b
	}
}

// jsonLookup devuelve el valor en path, si existe.
func jsonLookup(doc any, path []string) (any, bool) {
	cur := doc
	for _, k := range path {
		m, ok := cur.(map[string]any)
		if !ok {
			return nil, false
		}
		if cur, ok = m[k]; !ok {
			return nil, false
		}
	}
	return cur, true
}

// jsonSetPath fija path = v creando los objetos intermedios; un intermedio que
// no es objeto se reemplaza, igual que haría MergeJSON con el overlay.
func jsonSetPath(doc map[string]any, path []string, v any) {
	m := doc
	for _, k := range path[:len(path)-1] {
		next, ok := m[k].(map[string]any)
		if !ok {
			next = map[string]any{}
			m[k] = next
		}
		m = next
	}
	m[path[len(path)-1]] = v
}

// pathString es la ruta tal como se enseña: permissions.allow, env.FOO.
func pathString(p []string) string { return strings.Join(p, ".") }

// decodeJSONObject decodifica un documento que tiene que ser un objeto JSON
// (json.Valid primero: Decode aceptaría basura detrás del primer valor).
func decodeJSONObject(data []byte) (map[string]any, error) {
	if !json.Valid(data) {
		return nil, fmt.Errorf("JSON inválido")
	}
	v, err := unmarshalJSONValue(data)
	if err != nil {
		return nil, err
	}
	m, ok := v.(map[string]any)
	if !ok {
		return nil, fmt.Errorf("no es un objeto JSON")
	}
	return m, nil
}
```

`decodeOverlayObject`, que necesita `bytes`, llega en la Task 5 junto con su primer uso. Si llegara antes,
el linter `unused` pararía este commit.

- [ ] **Step 3: `stripAutoLayer` — `internal/core/autohooks.go`, junto a `keepForeignStopFailure`**

```go
// stripAutoLayer devuelve una copia de un settings.json decodificado sin lo que
// es de ccp: el statusLine si es nuestro envoltorio y las entradas de
// hooks.StopFailure que llaman a `_limit-hook`. El evento y `hooks` desaparecen
// si se quedan vacíos.
//
// Lo usa la deriva (cfg_drift.go): la capa es infraestructura, no una preferencia
// que el usuario cambiara con /config, así que nunca se adopta al overlay, ni
// siquiera cuando la ruta del binario difiere entre dos generaciones. El
// criterio es el mismo que isCCPStatusLine y keepForeignStopFailure, a propósito:
// tres sitios que decidieran distinto qué es «nuestro» acabarían envolviendo o
// borrando algo del usuario. No muta doc.
func stripAutoLayer(doc map[string]any) map[string]any {
	out := make(map[string]any, len(doc))
	for k, v := range doc {
		out[k] = v
	}
	if sl, ok := out["statusLine"].(map[string]any); ok {
		if cmd, _ := sl["command"].(string); isCCPStatusLine(strings.TrimSpace(cmd), AutoHooksBin()) {
			delete(out, "statusLine")
		}
	}
	hooks, ok := out["hooks"].(map[string]any)
	if !ok {
		return out
	}
	arr, ok := hooks[autoStopFailureEvent].([]any)
	if !ok {
		return out
	}
	kept := make([]any, 0, len(arr))
	for _, e := range arr {
		if b, err := json.Marshal(e); err == nil && strings.Contains(string(b), autoLimitHookCmd) {
			continue
		}
		kept = append(kept, e)
	}
	h := make(map[string]any, len(hooks))
	for k, v := range hooks {
		h[k] = v
	}
	if len(kept) == 0 {
		delete(h, autoStopFailureEvent)
	} else {
		h[autoStopFailureEvent] = kept
	}
	if len(h) == 0 {
		delete(out, "hooks")
	} else {
		out["hooks"] = h
	}
	return out
}
```

- [ ] **Step 4: Comprobar**

Run: `go test ./internal/core/ && gofmt -l internal cmd && go vet ./... && go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run --timeout 5m`
Expected: PASS.

- [ ] **Step 5: Commit**

`feat(cfg): motor puro para detectar la deriva de settings.json (B6)`

---

### Task 4: B6 (2/4) — la copia de lo último generado

Sin línea base no se puede distinguir «el usuario cambió esto» de «el global cambió». Esta tarea solo
guarda la copia y separa la construcción de la escritura. Todavía no adopta nada.

**Files:**
- Modify: `internal/core/cfg.go` (`cfgBuildSettings`, `cfgMergeSettings`, rutas de estado, `saveLastSettings`)
- Modify: `internal/core/cfg_test.go`
- Modify: `internal/core/snapshot_layout_test.go` (la lista de lo prohibido)

- [ ] **Step 1: Los tests**

`cfg_test.go`:

```go
// La copia de lo generado es la línea base de la deriva de /config (B6): tiene
// que ser byte a byte lo que se escribió en cc-home, y no es de nadie más (0600).
func TestCfgRegenerateGuardaLoGenerado(t *testing.T) {
	home, src := t.TempDir(), t.TempDir()
	t.Setenv("CCP_CLAUDE_SRC", src)
	if err := os.WriteFile(filepath.Join(src, "settings.json"), []byte(`{"model":"opus"}`), 0o644); err != nil {
		t.Fatal(err)
	}
	if err := ProfileAddOfficial(home, "work"); err != nil {
		t.Fatal(err)
	}
	gen, err := os.ReadFile(filepath.Join(ccHomePath(home, "work"), "settings.json"))
	if err != nil {
		t.Fatal(err)
	}
	last, err := os.ReadFile(lastSettingsPath(home, "work"))
	if err != nil || !bytes.Equal(gen, last) {
		t.Fatalf("copia = %q (%v), generado = %q", last, err, gen)
	}
	if fi, _ := os.Stat(lastSettingsPath(home, "work")); fi.Mode().Perm() != 0o600 {
		t.Errorf("modo de la copia = %v, quiero 0600", fi.Mode().Perm())
	}
	if !strings.HasPrefix(lastSettingsPath(home, "work"), profileDirPath(home, "work")+string(filepath.Separator)) ||
		strings.Contains(lastSettingsPath(home, "work"), "cc-home") || strings.Contains(lastSettingsPath(home, "work"), "overlay") {
		t.Errorf("la copia tiene que vivir en profiles/<n>/, fuera de cc-home/ y de overlay/: %s", lastSettingsPath(home, "work"))
	}
}

// Si la copia no se puede escribir, la regeneración lo dice: una copia desfasada
// haría adoptar como deriva los cambios del global.
func TestCfgRegenerateFallaSiNoPuedeGuardarLaCopia(t *testing.T) {
	home, src := t.TempDir(), t.TempDir()
	t.Setenv("CCP_CLAUDE_SRC", src)
	if err := ProfileAddOfficial(home, "work"); err != nil {
		t.Fatal(err)
	}
	st := profileStateDir(home, "work")
	if err := os.RemoveAll(st); err != nil {
		t.Fatal(err)
	}
	if err := os.WriteFile(st, []byte("no soy un directorio"), 0o644); err != nil {
		t.Fatal(err)
	}
	if err := CfgRegenerate(home, "work", src); err == nil {
		t.Fatal("sin poder guardar la copia, CfgRegenerate tenía que fallar")
	}
}
```

`snapshot_layout_test.go`, en `TestSnapshotSources`: añade `"last-settings"` a la lista `forbidden`. Tras
la Task 1 el fixture ya regenera, así que la copia existe.

Run: `go test ./internal/core/ -run 'GuardaLoGenerado|NoPuedeGuardarLaCopia|SnapshotSources'`
Expected: FAIL de compilación (`lastSettingsPath` no existe).

- [ ] **Step 2: El arreglo — `internal/core/cfg.go`**

Rutas, junto a las del overlay:

```go
// profileStateDir es el estado derivado de un perfil que no es de Claude Code.
// No vive en el cc-home, porque Claude Code lo lee entero, ni en overlay/, que
// es authored y lo capturan snapshots y backups: SnapshotSources y BackupExport
// enumeran rutas explícitas y esta no está. Cuelga de profiles/<n>/, así que se
// mueve con el rename y se borra con el rm.
func profileStateDir(home, name string) string {
	return filepath.Join(profileDirPath(home, name), "state")
}

// lastSettingsPath es la copia de lo último que ccp escribió en
// cc-home/settings.json: la línea base de la deriva de /config (cfg_drift.go).
func lastSettingsPath(home, name string) string {
	return filepath.Join(profileStateDir(home, name), "last-settings.json")
}

// invalidSettingsPath guarda un cc-home/settings.json que no era JSON antes de
// regenerarlo encima (cfg_drift.go).
func invalidSettingsPath(home, name string) string {
	return filepath.Join(profileStateDir(home, name), "settings.invalid.json")
}
```

`cfgMergeSettings` se parte en dos. La construcción queda sin escritura, porque la Task 5 la reutiliza para
saber qué saldría sin adoptar nada:

```go
// cfgBuildSettings construye el settings.json de un perfil (global ⊕ overlay ⊕
// auto) sin escribirlo. El global se ignora si falta o no es JSON válido.
func cfgBuildSettings(home, name, src string, overlayData []byte) ([]byte, error) {
	var globalData []byte
	if g, err := os.ReadFile(filepath.Join(src, "settings.json")); err == nil && json.Valid(g) {
		globalData = g
	}
	merged, err := MergeJSON(globalData, overlayData)
	if err != nil {
		return nil, fmt.Errorf("merge de settings de %q falló: %w", name, err)
	}
	// (el comentario de la tercera capa, tal cual está hoy en cfgMergeSettings)
	return applyAutoLayer(home, name, merged), nil
}
```

`cfgMergeSettings` lee el overlay, llama a `cfgBuildSettings`, escribe `settings.json` como hoy y termina
con `return saveLastSettings(home, name, merged)`:

```go
// saveLastSettings guarda la copia de lo que se acaba de escribir en el cc-home.
// Si no puede, borra la vieja y lo dice: una copia desfasada es peor que
// ninguna, porque la siguiente regeneración tomaría por deriva los cambios del
// global y los congelaría en el overlay. Sin copia, simplemente no se adopta.
func saveLastSettings(home, name string, data []byte) error {
	p := lastSettingsPath(home, name)
	if err := writeFileAtomic(p, data, 0o600); err != nil {
		_ = os.Remove(p)
		return fmt.Errorf("no se pudo guardar la copia de lo generado para %q: %w", name, err)
	}
	return nil
}
```

`writeFileAtomic` (`transcript.go`) ya crea el directorio y usa un temporal `ccp-*.tmp` en el mismo sitio.

- [ ] **Step 3: Comprobar**

Run: `go test ./internal/... && gofmt -l internal cmd && go vet ./...`
Expected: PASS.

- [ ] **Step 4: Commit**

`feat(cfg): guardar la copia de lo último generado en profiles/<n>/state (B6)`

---

### Task 5: B6 (3/4) — la adopción dentro de `CfgRegenerate`

**Files:**
- Modify: `internal/core/cfg_drift.go` (`SettingsDrift`, `adoptSettingsDrift`, `writeOverlayPreservingLink`)
- Modify: `internal/core/cfg.go` (`cfgMergeSettings` devuelve la deriva; `CfgRegenerateReport`; `CfgRegenerate` envoltorio)
- Modify: `internal/core/cfg_drift_test.go`

- [ ] **Step 1: Los tests (`cfg_drift_test.go`)**

```go
// perfilConBase deja el perfil «work» recién creado: tras B8, el alta ya generó
// su settings.json y la copia, que es la línea base de la deriva.
func perfilConBase(t *testing.T, global, overlay string) (home, src string) {
	t.Helper()
	home, src = t.TempDir(), t.TempDir()
	t.Setenv("CCP_CLAUDE_SRC", src)
	if global != "" {
		if err := os.WriteFile(filepath.Join(src, "settings.json"), []byte(global), 0o644); err != nil {
			t.Fatal(err)
		}
	}
	if err := ProfileAddOfficial(home, "work"); err != nil {
		t.Fatal(err)
	}
	if overlay != "" {
		if err := os.WriteFile(cfgSettingsFile(home, "work"), []byte(overlay), 0o644); err != nil {
			t.Fatal(err)
		}
		if err := CfgRegenerate(home, "work", src); err != nil {
			t.Fatal(err)
		}
	}
	return home, src
}

// simulaConfig hace lo que /config de Claude Code: lee cc-home/settings.json,
// cambia algo y lo reescribe entero con su propio formato (sin el orden ni la
// sangría de ccp, y con los números normalizados).
func simulaConfig(t *testing.T, home string, mutate func(map[string]any)) {
	t.Helper()
	p := filepath.Join(ccHomePath(home, "work"), "settings.json")
	data, err := os.ReadFile(p)
	if err != nil {
		t.Fatal(err)
	}
	var m map[string]any
	if err := json.Unmarshal(data, &m); err != nil {
		t.Fatal(err)
	}
	mutate(m)
	out, err := json.Marshal(m)
	if err != nil {
		t.Fatal(err)
	}
	if err := os.WriteFile(p, out, 0o644); err != nil {
		t.Fatal(err)
	}
}

func leeOverlay(t *testing.T, home string) map[string]any {
	t.Helper()
	data, err := os.ReadFile(cfgSettingsFile(home, "work"))
	if err != nil {
		t.Fatal(err)
	}
	m, err := decodeOverlayObject(data)
	if err != nil {
		t.Fatal(err)
	}
	return m
}

func leeGenerado(t *testing.T, home string) map[string]any {
	t.Helper()
	data, err := os.ReadFile(filepath.Join(ccHomePath(home, "work"), "settings.json"))
	if err != nil {
		t.Fatal(err)
	}
	return obj(t, string(data))
}

func TestDerivaDeConfigSeAdoptaEnElOverlay(t *testing.T) {
	home, src := perfilConBase(t, `{"cleanupPeriodDays":30.0,"permissions":{"allow":["Bash(ls)"]}}`, `{"env":{"O":"1"}}`)
	simulaConfig(t, home, func(m map[string]any) {
		m["autoCompactEnabled"] = false
		p := m["permissions"].(map[string]any)
		p["allow"] = append(p["allow"].([]any), "Bash(make)")
	})
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil {
		t.Fatal(err)
	}
	if want := []string{"autoCompactEnabled", "permissions.allow"}; !reflect.DeepEqual(d.Adopted, want) {
		t.Fatalf("Adopted = %v, quiero %v (el 30.0 → 30 de /config no es deriva)", d.Adopted, want)
	}
	ov := leeOverlay(t, home)
	if v, _ := jsonLookup(ov, []string{"env", "O"}); v != "1" {
		t.Errorf("se perdió lo que ya tenía el overlay: %v", ov)
	}
	if v, _ := jsonLookup(ov, []string{"autoCompactEnabled"}); v != false {
		t.Errorf("autoCompactEnabled no llegó al overlay: %v", ov)
	}
	if v, _ := jsonLookup(ov, []string{"permissions", "allow"}); len(v.([]any)) != 2 {
		t.Errorf("permissions.allow no llegó entero al overlay: %v", ov)
	}
	if v, _ := jsonLookup(leeGenerado(t, home), []string{"autoCompactEnabled"}); v != false {
		t.Error("tras regenerar, cc-home tiene que seguir con el cambio de /config")
	}
	if d, err := CfgRegenerateReport(home, "work", src); err != nil || !d.Empty() {
		t.Errorf("la segunda regeneración no tiene nada que adoptar: %+v %v", d, err)
	}
}

func TestDerivaQuitadaSoloSeAvisa(t *testing.T) {
	home, src := perfilConBase(t, "", `{"env":{"O":"1"}}`)
	simulaConfig(t, home, func(m map[string]any) { delete(m["env"].(map[string]any), "O") })
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil {
		t.Fatal(err)
	}
	if !reflect.DeepEqual(d.Removed, []string{"env.O"}) || len(d.Adopted) != 0 {
		t.Fatalf("deriva = %+v, quiero Removed [env.O] y nada adoptado", d)
	}
	if v, _ := jsonLookup(leeOverlay(t, home), []string{"env", "O"}); v != "1" {
		t.Error("un borrado no se puede expresar en el overlay: no se toca")
	}
}

func TestDerivaConflictoGanaElOverlay(t *testing.T) {
	home, src := perfilConBase(t, "", `{"model":"a"}`)
	simulaConfig(t, home, func(m map[string]any) { m["model"] = "b" })
	if err := os.WriteFile(cfgSettingsFile(home, "work"), []byte(`{"model":"c"}`), 0o644); err != nil {
		t.Fatal(err)
	}
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil {
		t.Fatal(err)
	}
	if !reflect.DeepEqual(d.Conflicts, []string{"model"}) || len(d.Adopted) != 0 {
		t.Fatalf("deriva = %+v, quiero Conflicts [model]", d)
	}
	if v, _ := jsonLookup(leeGenerado(t, home), []string{"model"}); v != "c" {
		t.Errorf("gana el overlay recién editado: model = %v", v)
	}
}

// Solo se adopta lo que la regeneración perdería: si el valor ya sale del global,
// copiarlo al overlay solo congelaría el global en el perfil.
func TestDerivaQueYaSaleNoSeAdopta(t *testing.T) {
	home, src := perfilConBase(t, `{"model":"opus"}`, "")
	simulaConfig(t, home, func(m map[string]any) { m["model"] = "sonnet" })
	if err := os.WriteFile(filepath.Join(src, "settings.json"), []byte(`{"model":"sonnet"}`), 0o644); err != nil {
		t.Fatal(err)
	}
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil || !d.Empty() {
		t.Fatalf("deriva = %+v %v, quiero vacía", d, err)
	}
	if _, ok := leeOverlay(t, home)["model"]; ok {
		t.Error("el overlay no tenía que ganar model")
	}
}

func TestDerivaSettingsInvalidoNoAdopta(t *testing.T) {
	home, src := perfilConBase(t, "", `{"env":{"O":"1"}}`)
	antes, _ := os.ReadFile(cfgSettingsFile(home, "work"))
	p := filepath.Join(ccHomePath(home, "work"), "settings.json")
	if err := os.WriteFile(p, []byte("{roto"), 0o644); err != nil {
		t.Fatal(err)
	}
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil {
		t.Fatal(err)
	}
	if d.Invalid != invalidSettingsPath(home, "work") || len(d.Adopted) != 0 {
		t.Fatalf("deriva = %+v", d)
	}
	if c, _ := os.ReadFile(d.Invalid); string(c) != "{roto" {
		t.Errorf("la copia del inválido = %q", c)
	}
	if ahora, _ := os.ReadFile(cfgSettingsFile(home, "work")); !bytes.Equal(antes, ahora) {
		t.Error("con un settings.json inválido el overlay no se toca")
	}
	if g, _ := os.ReadFile(p); !json.Valid(g) {
		t.Error("tras avisar, se regenera")
	}
}

func TestDerivaSinCopiaPreviaNoAdopta(t *testing.T) {
	home, src := perfilConBase(t, "", "")
	if err := os.Remove(lastSettingsPath(home, "work")); err != nil {
		t.Fatal(err)
	}
	simulaConfig(t, home, func(m map[string]any) { m["model"] = "x" })
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil || !d.Empty() {
		t.Fatalf("sin línea base no se atribuye nada: %+v %v", d, err)
	}
	if !fileExists(lastSettingsPath(home, "work")) {
		t.Error("se guarda la copia para la próxima vez")
	}
}

// La capa auto está en las dos copias y es de ccp: nunca se adopta, ni cuando la
// ruta del binario difiere entre lo generado y lo que hay ahora.
func TestDerivaNoAdoptaLaCapaAuto(t *testing.T) {
	SetAutoHooksBin("/viejo/bin/ccp")
	t.Cleanup(func() { SetAutoHooksBin("ccp") })
	home, src := perfilConBase(t, `{"statusLine":{"type":"command","command":"starship prompt"}}`, "")
	c, err := Load(home)
	if err != nil {
		t.Fatal(err)
	}
	c.AutoHandoff = &AutoHandoff{Hooks: []string{"work"}}
	if err := Save(home, c); err != nil {
		t.Fatal(err)
	}
	if err := CfgRegenerate(home, "work", src); err != nil {
		t.Fatal(err)
	}
	simulaConfig(t, home, func(m map[string]any) {
		m["autoCompactEnabled"] = false
		sl := m["statusLine"].(map[string]any)
		sl["command"] = strings.Replace(sl["command"].(string), "/viejo/bin/ccp", "/nuevo/bin/ccp", 1)
	})
	d, err := CfgRegenerateReport(home, "work", src)
	if err != nil {
		t.Fatal(err)
	}
	if !reflect.DeepEqual(d.Adopted, []string{"autoCompactEnabled"}) {
		t.Fatalf("Adopted = %v, quiero solo autoCompactEnabled", d.Adopted)
	}
	ov := leeOverlay(t, home)
	for _, k := range []string{"statusLine", "hooks"} {
		if _, ok := ov[k]; ok {
			t.Errorf("la capa auto (%s) acabó en el overlay: %v", k, ov)
		}
	}
	if sl, _ := jsonLookup(leeGenerado(t, home), []string{"statusLine", "command"}); !strings.Contains(fmt.Sprint(sl), "_statusline") {
		t.Errorf("la capa se sigue generando: %v", sl)
	}
}

// Un overlay que es un symlink (un repo de dotfiles) sigue siéndolo tras adoptar.
func TestDerivaRespetaUnOverlaySymlink(t *testing.T) {
	home, src := perfilConBase(t, "", "")
	dotfiles := filepath.Join(t.TempDir(), "work.json")
	if err := os.WriteFile(dotfiles, []byte("{}\n"), 0o644); err != nil {
		t.Fatal(err)
	}
	ovPath := cfgSettingsFile(home, "work")
	if err := os.Remove(ovPath); err != nil {
		t.Fatal(err)
	}
	if err := os.Symlink(dotfiles, ovPath); err != nil {
		t.Fatal(err)
	}
	if err := CfgRegenerate(home, "work", src); err != nil {
		t.Fatal(err)
	}
	simulaConfig(t, home, func(m map[string]any) { m["model"] = "x" })
	if _, err := CfgRegenerateReport(home, "work", src); err != nil {
		t.Fatal(err)
	}
	if !isSymlink(ovPath) {
		t.Fatal("el overlay dejó de ser un symlink")
	}
	if data, _ := os.ReadFile(dotfiles); !strings.Contains(string(data), `"model"`) {
		t.Errorf("la adopción no llegó al destino del enlace: %s", data)
	}
}
```

Run: `go test ./internal/core/ -run Deriva`
Expected: FAIL de compilación (`CfgRegenerateReport` no existe).

- [ ] **Step 2: El arreglo — `cfg_drift.go` (la parte con disco)**

```go
// SettingsDrift es lo que ccp encontró cambiado a mano en el cc-home/settings.json
// de un perfil al regenerarlo, casi siempre con /config, /model o /permissions.
type SettingsDrift struct {
	Profile   string
	Adopted   []string // claves copiadas al overlay: sobreviven a esta regeneración y a las siguientes
	Removed   []string // quitadas a mano, pero salen del global o del overlay y vuelven: solo se avisa
	Conflicts []string // cambiadas a mano y también en el overlay desde la última regeneración: gana el overlay
	Invalid   string   // != "": settings.json no era JSON; su copia está aquí y no se adoptó nada
}

// Empty dice si no hay nada que contar.
func (d SettingsDrift) Empty() bool {
	return len(d.Adopted) == 0 && len(d.Removed) == 0 && len(d.Conflicts) == 0 && d.Invalid == ""
}

// adoptSettingsDrift compara el cc-home/settings.json de ahora con la copia de lo
// último generado y pasa al overlay lo que el usuario añadió o cambió, antes de
// que la regeneración lo pise. Reglas (plan 2026-09-19, «Decisiones»):
//   - sin copia, o con una copia ilegible: no se atribuye nada;
//   - cc-home inválido: se guarda una copia y no se adopta nada;
//   - la capa auto no es deriva (stripAutoLayer);
//   - solo se adopta lo que la regeneración perdería;
//   - si el overlay también cambió esa clave, gana el overlay;
//   - lo quitado solo se avisa, y solo si va a volver.
func adoptSettingsDrift(home, name, src string) (SettingsDrift, error) {
	d := SettingsDrift{Profile: name}
	lastB, err := os.ReadFile(lastSettingsPath(home, name))
	if err != nil {
		return d, nil
	}
	curB, err := os.ReadFile(filepath.Join(ccHomePath(home, name), "settings.json"))
	if err != nil {
		return d, nil // nadie lo escribió: nada que adoptar
	}
	cur, err := decodeJSONObject(curB)
	if err != nil {
		inv := invalidSettingsPath(home, name)
		if werr := writeFileAtomic(inv, curB, 0o600); werr != nil {
			// Sin copia no se regenera encima: sería perder lo que el usuario escribió.
			return d, fmt.Errorf("cc-home/settings.json de %q no es JSON válido y no se pudo guardar una copia antes de regenerar: %w", name, werr)
		}
		d.Invalid = inv
		return d, nil
	}
	last, err := decodeJSONObject(lastB)
	if err != nil {
		return d, nil // la copia es estado derivado: ilegible = no hay línea base
	}
	file := cfgSettingsFile(home, name)
	ovB, err := os.ReadFile(file)
	if err != nil {
		return d, fmt.Errorf("no se pudo leer overlay de settings de %q: %w", name, err)
	}
	ov, err := decodeOverlayObject(ovB)
	if err != nil {
		return d, nil // overlay roto: cfgMergeSettings falla justo después y no pisa nada
	}
	expB, err := cfgBuildSettings(home, name, src, ovB)
	if err != nil {
		return d, nil
	}
	exp, err := decodeJSONObject(expB)
	if err != nil {
		return d, nil
	}

	last, cur = stripAutoLayer(last), stripAutoLayer(cur)
	for _, ch := range diffSettings(last, cur) {
		p := pathString(ch.Path)
		if ch.Kind == changeRemoved {
			if _, back := jsonLookup(exp, ch.Path); back {
				d.Removed = append(d.Removed, p)
			}
			continue
		}
		if v, ok := jsonLookup(exp, ch.Path); ok && jsonEqual(v, ch.Value) {
			continue // ya sale así: adoptarlo solo congelaría el global en el overlay
		}
		if v, ok := jsonLookup(ov, ch.Path); ok {
			if lv, lok := jsonLookup(last, ch.Path); !lok || !jsonEqual(v, lv) {
				d.Conflicts = append(d.Conflicts, p)
				continue
			}
		}
		jsonSetPath(ov, ch.Path, ch.Value)
		d.Adopted = append(d.Adopted, p)
	}
	if len(d.Adopted) == 0 {
		return d, nil
	}
	out, err := marshalIndent(ov)
	if err != nil {
		return d, fmt.Errorf("no se pudo serializar el overlay de %q: %w", name, err)
	}
	if err := writeOverlayPreservingLink(file, out); err != nil {
		return d, fmt.Errorf("no se pudo guardar en el overlay de %q lo adoptado de /config: %w", name, err)
	}
	return d, nil
}

// writeOverlayPreservingLink reescribe el overlay con tmp+rename sin romper un
// symlink (un repo de dotfiles): escribe en su destino y con su modo.
func writeOverlayPreservingLink(file string, data []byte) error {
	target := file
	if t, err := filepath.EvalSymlinks(file); err == nil {
		target = t
	}
	perm := os.FileMode(0o644)
	if fi, err := os.Stat(target); err == nil {
		perm = fi.Mode().Perm()
	}
	return writeFileAtomic(target, data, perm)
}

// decodeOverlayObject es decodeJSONObject aceptando el overlay en blanco, que
// MergeJSON trata como ausente.
func decodeOverlayObject(data []byte) (map[string]any, error) {
	if len(bytes.TrimSpace(data)) == 0 {
		return map[string]any{}, nil
	}
	return decodeJSONObject(data)
}
```

Añade `"bytes"`, `"os"` y `"path/filepath"` a los imports de `cfg_drift.go`. En los tests, añade los que
falten a `cfg_drift_test.go` (`encoding/json`, `fmt`, `reflect`…) y `bytes` a `cfg_test.go`. El lint
incluye `goimports`.

- [ ] **Step 3: El arreglo — `cfg.go`**

`cfgMergeSettings` pasa a `func cfgMergeSettings(home, name, src string) (SettingsDrift, error)`:

```go
	// Primero la deriva: lo que /config escribió en cc-home pasa al overlay ANTES
	// de leerlo para el merge, o la escritura de abajo lo pisaría (B6).
	drift, err := adoptSettingsDrift(home, name, src)
	if err != nil {
		return drift, err
	}
	// …lectura del overlay, cfgBuildSettings y escritura de settings.json, como en la Task 4…
	if err := saveLastSettings(home, name, merged); err != nil {
		return drift, err
	}
	return drift, nil
```

```go
// CfgRegenerate regenera el cc-home efectivo … (el comentario de hoy). Adopta la
// deriva de /config igual que CfgRegenerateReport; solo no la cuenta.
func CfgRegenerate(home, name, src string) error {
	_, err := CfgRegenerateReport(home, name, src)
	return err
}

// CfgRegenerateReport es CfgRegenerate devolviendo lo que encontró cambiado a
// mano en cc-home/settings.json (B6). La adopción ocurre en TODOS los caminos
// que regeneran (sync, config, instruct, la GUI, los restores): si solo la
// hiciera `profile sync`, cualquier otro camino perdería el /config igual que
// antes. Contarlo es lo opcional.
func CfgRegenerateReport(home, name, src string) (SettingsDrift, error) {
	if err := CfgInitOverlay(home, name); err != nil {
		return SettingsDrift{Profile: name}, err
	}
	if err := cfgWriteClaudeMD(home, name, src); err != nil {
		return SettingsDrift{Profile: name}, err
	}
	return cfgMergeSettings(home, name, src)
}
```

- [ ] **Step 4: Comprobar**

Run: `go test ./internal/... && gofmt -l internal cmd && go vet ./... && go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run --timeout 5m`
Expected: PASS. En particular siguen verdes `TestCfgRegenerate*`, `TestProfileConfig*`, `TestBackupRestore*`,
`TestSnapshot*`, los de `autohooks` y los de `effective`: ninguno escribe a mano en `cc-home/settings.json`
entre dos regeneraciones.

- [ ] **Step 5: Commit**

`fix(cfg): lo que se cambia con /config dentro de un perfil se adopta en su overlay (B6)`

---

### Task 6: B6 (4/4) — `profile sync` y `serve` dicen qué se adoptó

**Files:**
- Modify: `internal/core/cfg_cmd.go` (`ProfileSyncReport`; `ProfileSync` pasa a envoltorio)
- Modify: `internal/core/cfg_test.go`
- Modify: `internal/cli/profile.go` (rama `sync`, `printSettingsDrift`), `internal/cli/cli_test.go`
- Modify: `internal/cli/serve_methods.go` (`srvProfilesSync`), `internal/cli/serve_test.go`
- Modify: `internal/core/i18n/catalog_cli.go`

La TUI no cambia. Su `sync` adopta igual, porque pasa por `CfgRegenerate`; el detalle está en
`ccp profile sync`.

- [ ] **Step 1: Los tests**

`cfg_test.go`:

```go
func TestProfileSyncReportSoloLosPerfilesConDeriva(t *testing.T) {
	home, src := t.TempDir(), t.TempDir()
	t.Setenv("CCP_CLAUDE_SRC", src)
	for _, n := range []string{"a", "b"} {
		if err := ProfileAddOfficial(home, n); err != nil {
			t.Fatal(err)
		}
	}
	p := filepath.Join(ccHomePath(home, "b"), "settings.json")
	if err := os.WriteFile(p, []byte(`{"autoCompactEnabled":false}`), 0o644); err != nil {
		t.Fatal(err)
	}
	ds, err := ProfileSyncReport(home, "")
	if err != nil {
		t.Fatal(err)
	}
	if len(ds) != 1 || ds[0].Profile != "b" || !reflect.DeepEqual(ds[0].Adopted, []string{"autoCompactEnabled"}) {
		t.Fatalf("informe = %+v", ds)
	}
	if ds, err := ProfileSyncReport(home, "a"); err != nil || len(ds) != 0 {
		t.Fatalf("un perfil sin deriva no sale: %+v %v", ds, err)
	}
}
```

`cli_test.go`:

```go
func TestDispatchProfileSyncEnseñaLaDeriva(t *testing.T) {
	home := t.TempDir()
	t.Setenv("CCP_HOME", home)
	t.Setenv("CCP_CLAUDE_SRC", t.TempDir())
	t.Setenv("CCP_LANG", "es")
	if err := core.ProfileAddOfficial(home, "work"); err != nil {
		t.Fatal(err)
	}
	sj := filepath.Join(home, "profiles", "work", "cc-home", "settings.json")
	if err := os.WriteFile(sj, []byte(`{"autoCompactEnabled":false}`), 0o644); err != nil {
		t.Fatal(err)
	}
	var out, errb bytes.Buffer
	if code := Dispatch([]string{"profile", "sync", "work"}, &out, &errb); code != 0 {
		t.Fatalf("exit %d: %s", code, errb.String())
	}
	if !strings.Contains(out.String(), "[ok] work: guardado en su overlay lo que cambiaste con /config: autoCompactEnabled") {
		t.Errorf("no cuenta lo adoptado: %q", out.String())
	}

	if err := os.WriteFile(sj, []byte("{roto"), 0o644); err != nil {
		t.Fatal(err)
	}
	out.Reset()
	if code := Dispatch([]string{"profile", "sync", "work"}, &out, &errb); code != 0 {
		t.Fatalf("exit %d: %s", code, errb.String())
	}
	if !strings.Contains(out.String(), "[warn] work:") || !strings.Contains(out.String(), "settings.invalid.json") {
		t.Errorf("no avisa del settings.json inválido: %q", out.String())
	}
}
```

`serve_test.go`:

```go
func TestServeProfilesSyncDevuelveLaDeriva(t *testing.T) {
	home := serveEnv(t)
	_, r := serveRun(t, req(1, "profiles.sync", map[string]any{"name": "work"}))
	var vacio struct {
		Drift []json.RawMessage `json:"drift"`
	}
	mustResult(t, r["1"], &vacio)
	if vacio.Drift == nil || len(vacio.Drift) != 0 {
		t.Fatalf("sin deriva, drift es [] y nunca null: %s", r["1"].Result)
	}
	sj := filepath.Join(home, "profiles", "work", "cc-home", "settings.json")
	if err := os.WriteFile(sj, []byte(`{"autoCompactEnabled":false}`), 0o644); err != nil {
		t.Fatal(err)
	}
	_, r = serveRun(t, req(2, "profiles.sync", map[string]any{"name": "work"}))
	var got struct {
		OK    bool `json:"ok"`
		Drift []struct {
			Profile   string   `json:"profile"`
			Adopted   []string `json:"adopted"`
			Removed   []string `json:"removed"`
			Conflicts []string `json:"conflicts"`
			Invalid   string   `json:"invalid"`
		} `json:"drift"`
	}
	mustResult(t, r["2"], &got)
	if !got.OK || len(got.Drift) != 1 || got.Drift[0].Profile != "work" ||
		!reflect.DeepEqual(got.Drift[0].Adopted, []string{"autoCompactEnabled"}) ||
		got.Drift[0].Removed == nil || got.Drift[0].Conflicts == nil {
		t.Fatalf("profiles.sync = %s", r["2"].Result)
	}
}
```

Si `serve_test.go` no importa `reflect`, añádelo.

Run: `go test ./internal/core/ ./internal/cli/ -run 'SyncReport|ProfileSyncEnseña|ProfilesSyncDevuelve'`
Expected: FAIL.

- [ ] **Step 2: Core — `cfg_cmd.go`**

```go
// ProfileSync … (el comentario de hoy). Adopta la deriva de /config igual que
// ProfileSyncReport; solo no la cuenta.
func ProfileSync(home, name string) error {
	_, err := ProfileSyncReport(home, name)
	return err
}

// ProfileSyncReport es ProfileSync devolviendo, por perfil y en orden, lo que la
// regeneración encontró cambiado a mano en cc-home/settings.json (B6). Solo salen
// los perfiles con algo que contar. Con error, devuelve lo reunido hasta ahí.
func ProfileSyncReport(home, name string) ([]SettingsDrift, error)
```

El cuerpo es el de hoy, cambiando cada `CfgRegenerate(home, n, src)` por:

```go
		d, err := CfgRegenerateReport(home, n, src)
		if !d.Empty() {
			out = append(out, d)
		}
		if err != nil {
			return out, err
		}
```

- [ ] **Step 3: CLI — catálogo y salida**

`catalog_cli.go`, junto a `cli.profile.synced_one`:

```go
	// La deriva de /config que `profile sync` encontró (B6). %s: perfil y claves.
	"cli.profile.sync_adopted": {
		En: "%s: kept in its overlay what you changed with /config: %s",
		Es: "%s: guardado en su overlay lo que cambiaste con /config: %s",
	},
	"cli.profile.sync_conflict": {
		En: "%s: %s changed with /config and also in the overlay; the overlay wins.",
		Es: "%s: %s cambió con /config y también en el overlay; gana el overlay.",
	},
	"cli.profile.sync_removed": {
		En: "%s: you removed %s with /config, but it comes from the global config or the overlay and is back: remove it where it comes from (overlay: ccp profile config %s).",
		Es: "%s: quitaste %s con /config, pero sale del global o del overlay y vuelve a estar: quítalo de donde sale (overlay: ccp profile config %s).",
	},
	"cli.profile.sync_invalid": {
		En: "%s: cc-home/settings.json was not valid JSON, so nothing from it was kept; it was regenerated and a copy is at %s.",
		Es: "%s: cc-home/settings.json no era JSON válido y no se guardó nada de él; se regeneró y hay una copia en %s.",
	},
```

`internal/cli/profile.go`, en la rama `sync`:

```go
		drifts, err := core.ProfileSyncReport(home, name)
		printSettingsDrift(stdout, lang, drifts)
		if err != nil {
			fmt.Fprintf(stderr, "[error] %v\n", err)
			return 1
		}
```

Deja las dos líneas `synced_all` / `synced_one` detrás, como hoy. `printSettingsDrift` va antes del error
para no esconder lo que sí se adoptó. Y la función:

```go
// printSettingsDrift cuenta lo que `profile sync` encontró cambiado a mano en el
// settings.json de cada perfil (casi siempre /config). Lo adoptado es un [ok]:
// ya está a salvo en el overlay. Lo demás es un [warn], porque decide el usuario.
func printSettingsDrift(w io.Writer, lang i18n.Lang, drifts []core.SettingsDrift) {
	for _, d := range drifts {
		if len(d.Adopted) > 0 {
			fmt.Fprintln(w, okLine(w, i18n.T(lang, "cli.profile.sync_adopted", d.Profile, strings.Join(d.Adopted, ", "))))
		}
		if len(d.Conflicts) > 0 {
			fmt.Fprintln(w, warnLine(w, i18n.T(lang, "cli.profile.sync_conflict", d.Profile, strings.Join(d.Conflicts, ", "))))
		}
		if len(d.Removed) > 0 {
			fmt.Fprintln(w, warnLine(w, i18n.T(lang, "cli.profile.sync_removed", d.Profile, strings.Join(d.Removed, ", "), d.Profile)))
		}
		if d.Invalid != "" {
			fmt.Fprintln(w, warnLine(w, i18n.T(lang, "cli.profile.sync_invalid", d.Profile, d.Invalid)))
		}
	}
}
```

- [ ] **Step 4: serve — `srvProfilesSync`**

```go
	drifts, err := core.ProfileSyncReport(s.home, p.Name)
	if err != nil {
		return nil, err
	}
	// drift: lo que /config había cambiado en cada perfil (B6). Siempre array, y
	// cada lista también, para que la GUI no tenga que distinguir null de vacío.
	type row struct {
		Profile   string   `json:"profile"`
		Adopted   []string `json:"adopted"`
		Removed   []string `json:"removed"`
		Conflicts []string `json:"conflicts"`
		Invalid   string   `json:"invalid"`
	}
	nz := func(v []string) []string {
		if v == nil {
			return []string{}
		}
		return v
	}
	out := []row{}
	for _, d := range drifts {
		out = append(out, row{Profile: d.Profile, Adopted: nz(d.Adopted), Removed: nz(d.Removed), Conflicts: nz(d.Conflicts), Invalid: d.Invalid})
	}
	return map[string]any{"ok": true, "drift": out}, nil
```

- [ ] **Step 5: Comprobar**

Run: `go test ./... && gofmt -l internal cmd && go vet ./... && go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run --timeout 5m && bash legacy/tests/run.sh && bash testdata/golden/capture.sh --check`
Expected: PASS.

- [ ] **Step 6: Commit**

`feat(profile): profile sync y profiles.sync cuentan lo adoptado de /config (B6)`

---

### Task 7: GUI — los avisos de sync y de rename

**Files:**
- Modify: `gui/src/lib/api.ts` (tipos de `renameProfile` y `syncProfile`, `SettingsDrift`)
- Modify: `gui/src/lib/actions.ts` (`renameModal`, `syncMsg`)
- Modify: `gui/src/screens/Perfiles.tsx` (el botón «Resincronizar todas»)
- Modify: `gui/src/lib/i18n_en.ts`

- [ ] **Step 1: `api.ts`**

```ts
/** Lo que /config había cambiado en el settings.json de un perfil (profiles.sync, B6). */
export interface SettingsDrift {
  profile: string;
  adopted: string[];
  removed: string[];
  conflicts: string[];
  invalid: string;
}
```

```ts
  renameProfile: (from: string, to: string) => ccpCall<{ ok: boolean; relogin: boolean }>('profiles.rename', { from, to }),
  syncProfile: (name = '') => ccpCall<{ ok: boolean; drift: SettingsDrift[] }>('profiles.sync', { name }),
```

- [ ] **Step 2: `actions.ts`**

Importa `type SettingsDrift` desde `./api`. En `renameModal`, dentro de `warns`, antes de las del lanzador:

```ts
      ...(p.type === 'official' && p.access === 'ok'
        ? [t('Claude Code guarda el login de esta cuenta según su carpeta: al renombrarla tendrás que volver a iniciar sesión.')]
        : []),
```

Y `onConfirm`:

```ts
    onConfirm: async (f) => {
      const to = f.to.trim();
      const r = await api.renameProfile(name, to);
      if (app.selected === name) app.select(to);
      return r.relogin
        ? t('Se renombró a {n}. Vuelve a iniciar sesión: ccp profile login {n}', { n: to })
        : t('Se renombró a {n}', { n: to });
    },
```

Nueva función exportada:

```ts
/** El aviso de «Resincronizar»: qué se guardó de /config en cada perfil y si quedan avisos (B6). */
export function syncMsg(r: { drift: SettingsDrift[] }): string {
  const drift = r.drift ?? [];
  const adopted = drift.filter((d) => d.adopted.length > 0).map((d) => `${d.profile}: ${d.adopted.join(', ')}`);
  let msg = adopted.length
    ? t('Cuentas resincronizadas. Se guardó en su perfil lo que cambiaste con /config: {k}', { k: adopted.join(' · ') })
    : t('Todas las cuentas resincronizadas');
  if (drift.some((d) => d.removed.length > 0 || d.conflicts.length > 0 || d.invalid !== '')) {
    msg += ' ' + t('Hay avisos: míralos con ccp profile sync.');
  }
  return msg;
}
```

- [ ] **Step 3: `Perfiles.tsx`**

Importa `syncMsg` junto a los demás de `../lib/actions` y cambia el botón:

```tsx
          onClick={() => mutate(() => api.syncProfile(''), { msg: syncMsg })}
```

- [ ] **Step 4: `i18n_en.ts`**

```ts
  "Claude Code guarda el login de esta cuenta según su carpeta: al renombrarla tendrás que volver a iniciar sesión.": "Claude Code keeps this account's login by its folder: after renaming it you'll have to log in again.",
  "Se renombró a {n}. Vuelve a iniciar sesión: ccp profile login {n}": "Renamed to {n}. Log in again: ccp profile login {n}",
  "Cuentas resincronizadas. Se guardó en su perfil lo que cambiaste con /config: {k}": "Accounts resynced. What you changed with /config was kept in each profile: {k}",
  "Hay avisos: míralos con ccp profile sync.": "There are warnings: see them with ccp profile sync.",
```

- [ ] **Step 5: Comprobar**

Run: `(cd gui && npm run typecheck)`
Expected: sin errores. Si faltan dependencias, `(cd gui && npm ci)` primero.

- [ ] **Step 6: Commit**

`feat(gui): avisos de sync (lo adoptado de /config) y de rename (volver a iniciar sesión)`

---

### Task 8: documentación y gates finales

**Files:**
- Modify: `CHANGELOG.md` (`## [Unreleased]` → `### Fixed`, una entrada por defecto, detrás de las de B1-B5)
- Modify: `CLAUDE.md`
- Modify: `docs/superpowers/specs/2026-09-18-config-unificada-snapshots-nube-design.md` (cabecera, §1 y §4)

- [ ] **Step 1: `CHANGELOG.md`**

```markdown
- **`ccp profile add` genera la config del perfil al crearlo**, como ya decía: `overlay/`,
  `cc-home/CLAUDE.md` y `cc-home/settings.json`. Antes el perfil nacía sin ellos hasta el primer
  `ccp profile sync` (el oráculo bash sí los generaba). También desde la TUI y la app de escritorio.
- **Lo que cambias con `/config` (o `/model`, `/permissions`…) dentro de un perfil ya no se pierde en el
  siguiente `ccp profile sync`.** Claude Code lo escribe en `cc-home/settings.json`, que ccp genera.
  Ahora ccp guarda una copia de lo último que generó y, antes de regenerar, pasa al overlay del perfil lo
  que añadiste o cambiaste.
  - Lo que quitaste solo se avisa: el overlay no puede expresar un borrado.
  - Si la clave cambió también en el overlay, gana el overlay y se avisa.
  - Si el archivo no es JSON válido, no se adopta nada y queda una copia.
  - Los sensores de `auto_handoff` nunca se adoptan.
  - `ccp profile sync` y `profiles.sync` de `ccp serve` dicen qué se adoptó.
  - Los perfiles creados antes empiezan a adoptar desde su primer sync, y `ccp upgrade` ya lo ejecuta.
- **`ccp profile rename` avisa de que hay que volver a iniciar sesión.** Claude Code guarda la credencial
  de un perfil official con un nombre que sale de la ruta de su cc-home (ADR 0016, M4), y el rename la
  mueve. ccp no toca el Llavero: dice que hace falta `ccp profile login <nuevo>`. Lo dicen también la TUI,
  `profiles.rename` de `ccp serve` (`relogin`) y la app de escritorio, que lo advierte antes de confirmar.
```

- [ ] **Step 2: `CLAUDE.md`**

- Bullet `profile.go`: añade «`ProfileAddOfficial`/`ProfileAddProvider` also generate the config right after
  seeding (`profileGenerateConfig` → `CfgRegenerate`: overlay + `cc-home/CLAUDE.md` + `settings.json`),
  like the oracle's `_seed_cc_home` (B8).» Añade también «`ProfileRename` returns `RenameResult{Relogin}`:
  official + `HasLogin` **before** moving. The Keychain name follows the cc-home path (M4); ccp never touches
  the Keychain, so all three front-ends print the hint (B7). `HasLogin` keeps saying "logged in" after a
  rename, because `oauthAccount` travels with `.claude.json`: the hint is the only signal.»
- Bullet `cfg.go`/`cfg_cmd.go`: añade un párrafo sobre la deriva (B6):
  - `CfgRegenerate` adopts, in **every** path that regenerates, what was changed by hand in
    `cc-home/settings.json` (usually `/config`) into the overlay before overwriting it.
  - The baseline is `profiles/<n>/state/last-settings.json`: a copy of what was last generated, `0600`,
    never captured by snapshots or backups. If it can't be written, the old one is deleted and the
    regeneration fails, because a stale baseline would freeze the global into the overlay.
  - Rules:
    - no baseline → nothing is adopted;
    - invalid JSON → copy to `state/settings.invalid.json` + warn;
    - the auto layer is stripped first (`stripAutoLayer`);
    - only what the regeneration would lose is adopted;
    - conflict with an edited overlay → the overlay wins;
    - removals are only warned.
  - `CfgRegenerateReport`/`ProfileSyncReport` return the `SettingsDrift`; `CfgRegenerate`/`ProfileSync`
    are wrappers.
  - The overlay is rewritten via tmp+rename, following a symlink.
- En «What a profile window actually reads»: cambia «so the next sync drops it (B6)» por «ccp now adopts it
  into the overlay on the next regeneration (B6)» y «loses the login (B7)» por «loses the login; the rename
  says so (B7)».
- En «Config & state locations», tras `profiles/<name>/overlay/`: «`profiles/<name>/state/` — derived
  state that is not Claude Code's: `last-settings.json` (baseline for the `/config` drift) and
  `settings.invalid.json`. Never captured; deleting it only turns adoption off until the next sync.»
- En `ccp serve`: «`profiles.sync` returns `drift` (always an array) and `profiles.rename` returns
  `relogin`, both next to `"ok": true`.»

- [ ] **Step 3: El spec**

- En la cabecera, en «Planes de implementación», añade tras la Fase 0:
  `- [Fase 0, cierre: defectos B6-B8](../plans/2026-09-19-fase0-b6-b8.md)`.
- §1: la nota `> **B1–B5 arreglados** …` pasa a decir B1–B8 y cita los dos planes. Añade las tres viñetas
  debajo de la de B5:
  - **B6 — `/config` dentro de un perfil se perdía** en el siguiente sync, porque escribe en el
    `cc-home/settings.json` que ccp genera. *Arreglado*: se adopta en el overlay (copia de lo generado en
    `profiles/<n>/state/`).
  - **B7 — `profile rename` dejaba el perfil sin login sin decirlo** (M4). *Arreglado*: avisa; ccp no
    toca el Llavero.
  - **B8 — `profile add` no generaba la config.** *Arreglado*: genera overlay, `CLAUDE.md` y
    `settings.json` como el oráculo.
- §4: en el párrafo «Arreglos: **B1–B5**, más **B6–B8**…», añade al final: «Los ocho, arreglados en la rama
  `fix/fase0-defectos`: B1-B5 con el plan 2026-09-18 y B6-B8 con el 2026-09-19.»

- [ ] **Step 4: Gates finales**

Run:

```bash
gofmt -l internal cmd
go vet ./...
go test ./...
go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run --timeout 5m
bash legacy/tests/run.sh
bash testdata/golden/capture.sh --check
(cd gui && npm run typecheck)
grep -n '^go ' go.mod    # go 1.24.0
git status --short       # solo lo de esta tarea
```

Expected: todo verde, y `gofmt` no imprime nada.

- [ ] **Step 5: Commit**

`docs: Fase 0, defectos B6-B8 (CHANGELOG, CLAUDE.md, spec)`

---

## Fuera de alcance

- **Mover o leer la credencial del Llavero en el rename.** ccp no toca secretos del Llavero; el aviso es
  el arreglo acordado (ADR 0016).
- **Que `HasLogin` detecte el login perdido tras un rename.** Haría falta mirar el Llavero.
- **Adoptar la deriva en perfiles sin copia previa.** Sin línea base, un cambio del usuario no se distingue
  de un cambio del global.
- **Enseñar el detalle de la deriva en la TUI.** Su `sync` adopta igual; el detalle lo da
  `ccp profile sync`.
- **Llevar la adopción al oráculo bash.** Está archivado y `profile sync` no es contrato congelado.

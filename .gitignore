*.tar.gz
*.zip
*.tmp
.DS_Store
.superpowers/

# Go build artifacts (binario en raíz por `go build ./cmd/ccp`)
# OJO: NO ignorar *.out — los golden de testdata/golden usan esa extensión.
/ccp
/dist/
*.test
coverage.out
